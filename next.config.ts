import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['countryflagsapi.com','flagcdn.com', 'cdn.pixabay.com','images.unsplash.com', 'plus.unsplash.com', 'img.freepik.com', 'flagsapi.com'],  // add your domain(s) here
  },
  /* config options here */
};

export default nextConfig;
